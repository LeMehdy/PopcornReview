<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Details</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="/static/styles.css" rel="stylesheet">
</head>
<body>
    <%- include("header.ejs"); %> 
    <div class="container mt-5">
        <h1>Movie Details</h1>

        <% if (movieDetails) { %>
            <div class="row mt-4">
                <div class="col-md-4">
                    <img src="https://image.tmdb.org/t/p/w500/<%= movieDetails.poster_path %>" alt="<%= movieDetails.title %> Poster" class="img-fluid">
                </div>
                <div class="col-md-8">
                    <h2><%= movieDetails.title %></h2>
                    <p><%= movieDetails.overview %></p>
                    <p>Release Date: <%= movieDetails.release_date %></p>
                    <p>Rating: <%= movieDetails.vote_average %></p>
                    <!-- Ajoutez des boutons pour noter, liker, ajouter à la watchlist, etc. -->
                    <div class="mt-3">
                        <button type="button" class="btn btn-primary me-2">Rate</button>
                        <button type="button" class="btn btn-secondary me-2">Like</button>
                        <button type="button" class="btn btn-info me-2">Add to Watchlist</button>
                    </div>
                    <!-- Section pour les commentaires -->
                    <div class="mt-4">
                        <h3>Comments</h3>
                        <!-- Formulaire pour ajouter un commentaire -->
                        <form>
                            <textarea class="form-control mb-2" rows="3" placeholder="Add a comment"></textarea>
                            <button type="submit" class="btn btn-primary">Submit</button>
                        </form>
                        <!-- Liste des commentaires -->
                        <div class="mt-3">
                            <div class="card">
                                <div class="card-body">
                                    <p class="card-text">A great movie!</p>
                                    <p class="card-text text-muted">Posted by User123</p>
                                </div>
                            </div>
                            <!-- Ajoutez plus de commentaires ici -->
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <p>No details available for this movie.</p>
        <% } %>

        <a href="/" class="btn btn-secondary mt-4">Back to Home</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/ejs@3.1.6/ejs.min.js"></script>
</body>
</html>
